# Base URL of the portfolio-api Cloudflare Worker.
# Required before running or building the site.
# After deploying the worker (see worker/README section in README.md),
# put its workers.dev URL here, e.g.:
# VITE_WORKER_API_URL=https://portfolio-api.your-subdomain.workers.dev
