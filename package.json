{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:web": "yarn workspace web dev",
    "build:web": "yarn workspace web build",
    "lint": "yarn workspaces run lint",
    "test:worker": "yarn workspace portfolio-api test"
  }
}
